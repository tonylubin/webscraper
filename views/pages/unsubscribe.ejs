<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include("../partials/head.ejs");  %> 
  </head>
  <body
    style="
      background-image: url('images/person-with-clipboard-near-laptop-shopping-trolley-magnifier.jpg');
      background-position: center;
      background-repeat: no-repeat;
      background-size: cover;
      height: 100vh;
    "
  >
    <div class="container-fluid" style="backdrop-filter: blur(6px); height: 100%;">
      <!-- NAVBAR -->
      <%- include("../partials/navbar.ejs")  %>
      <div class="row">
        <div class="col-12">
          <!-- HEADER -->
          <%- include("../partials/header.ejs");  %> 
        </div>
      </div>
      <div class="row justify-content-center">
        <p class="col-8 px-0 py-5 text-center text-dark">
          We're glad that you have saved some money with the use of The Money Saver Helper price alert app.
        </p>
      </div> 
      <main class="row justify-content-center">
        <form
          class="col-8 d-flex flex-column gap-4"
          action="/unsubscribe"
          method="post"
        >
          <label for="documentId" class="form-label fw-bold text-primary fs-5"
            >To Unsubscribe enter your Price Alert ID below:</label
          >
          <input
            type="text"
            class="form-control"
            name="documentId"
            id="documentId"
            placeholder="price alert ID number..."
            minlength="24"
            required
          />
          <button type="submit" class="btn btn-primary fw-bold">
            Unsubscribe
          </button>
        </form>
      </main>
    </div>
  </body>
</html>