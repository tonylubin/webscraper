<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include("../partials/head.ejs");  %> 
  </head>
  <body>
    <div class="container-fluid">
      <!-- NAVBAR -->
      <%- include("../partials/navbar.ejs")  %>
      <div class="row">
        <div class="col-12">
          <!-- HEADER -->
          <%- include("../partials/header.ejs");  %>
        </div>
      </div>
      <p class="row justify-content-center m-5 px-5 text-success gap-3 fw-bold">
        Congratulations, a price alert has been created and will be sent to: <span class="text-dark text-center fw-normal fs-5"> <%= userEmail %> </span> 
      </p>
      <main class="row justify-content-center" style="height: 100%">
        <h3 class="text-center text-primary pb-5">Here is your search result:</h3>
        <img src="<%= priceAlert.imageUrl %>" alt="image of <%= priceAlert.name %> " style="height: 400px; width: 400px;"/>
        <ul class="list-group align-items-center p-0 pt-5 gap-3">
            <li class="col list-group-item border-0 text-decoration-underline fs-5" style="font-weight: 600;"><%= priceAlert.name %> </li>
            <li class="col list-group-item border-0 fw-bold text-primary fs-5">Current Price: <%= priceAlert.currentPrice %> </li>
            <% if (priceAlert.previousPrice.length) { %>
                <li class="col list-group-item border-0 text-danger text-decoration-line-through fs-5">Previous Price: <%= priceAlert.previousPrice %></li> 
            <% } %>
            <% if (priceAlert.offer.length) { %>
                <li class="col list-group-item border border-danger text-danger">OFFER: <%= priceAlert.offer %> </li>
            <% } %>
            <% if (priceAlert.stockStatus === "Add to basket") { %>
                <li class="col list-group-item border-0 text-success fs-5 fw-bold">In Stock</li>
            <% } else  { %>
                <li class="col list-group-item border-0">Not In Stock</li>
            <% } %>
        </ul>
      </main>
      <div class="row justify-content-center pt-5">
        <p class="mt-3 px-5 text-center fs-5 text-primary text-decoration-underline">Below is the ID Number for the price alert you created:</p>
        <p class="text-center text-danger"><%= priceAlert._id %> </p>
        <p class="text-center text-secondary">(quote this number when you want to unsubscribe from this price alert)</p>
        <a class="text-center link-dark mb-3" href="/unsubscribe">
          http://localhost:5000/unsubscribe
        </a>
      </div>
    </div>
  </body>
</html>