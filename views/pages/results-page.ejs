<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include("../partials/head.ejs");  %> 
  </head>
  <body>
    <!-- HEADER -->
    <%- include("../partials/header.ejs");  %>
  </div>
    <div class="container-fluid d-flex flex-column justify-content-center pt-5 h-100">
      <!-- TITLE -->
      <%- include("../partials/title.ejs")  %>
      <p class="row justify-content-center m-5 px-5 text-success gap-3 fw-bold fs-5">
        Congratulations, a price alert has been created and will be sent to: <span class="text-dark text-center fw-bold fs-5">&#x2709; <%= userEmail %> </span> 
      </p>
      <main class="row justify-content-center h-100%">
        <p class="col-12 text-center orangeUnderline fs-4 fw-bold redText text-capitalize py-4"> <%= priceAlert.name %> </p>
        <img src="<%= priceAlert.imageUrl %>" alt="<%= priceAlert.name %>" style="height: 400px; width: 400px;" class="product_showcase p-5 border border-dark border-2"/>
        <ul class="list-group align-items-center p-0 pt-5 gap-4">
            <li class="col list-group-item rounded-pill fw-bold text-white bg-info fs-5">Current Price: <%= priceAlert.currentPrice %> </li>
            <% if (priceAlert.previousPrice.length) { %>
                <li class="col list-group-item border-0 text-danger text-decoration-line-through fs-5">Previous Price: <%= priceAlert.previousPrice %></li> 
            <% } %>
            <% if (priceAlert.offer.length) { %>
                <li class="col list-group-item border border-2 border-danger text-danger bg-transparent fw-semibold">OFFER: <%= priceAlert.offer %> </li>
            <% } %>
            <% if (priceAlert.stockStatus === "Add to basket") { %>
                <li class="col list-group-item border-0 text-success fs-5 fw-bold bg-transparent">In Stock</li>
            <% } else  { %>
                <li class="col list-group-item border-0 bg-transparent">Not In Stock</li>
            <% } %>
        </ul>
      </main>
      <div class="row justify-content-center pt-5">
        <p class="mt-3 px-5 text-center fs-5 text-decoration-underline">Below is the ID Number for the price alert you created:</p>
        <p class="text-center text-warning fw-bold"><%= priceAlert._id %> </p>
        <p class="text-center text-black-50">(quote this number when you want to unsubscribe from this price alert)</p>
        <a class="text-center link-primary mb-3" href="/unsubscribe">
          http://localhost:5000/unsubscribe
        </a>
      </div>
    </div>
  </body>
</html>