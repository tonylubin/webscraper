<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Price Saver Alert</title>
    <style>
      body {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      img {
        width: 400px;
        height: 400px;
        display: block;
        margin: auto;
      }

      main {
        width: 80vw;
        margin: auto;
      }

      main > p {
        font-size: 1.25rem;
        font-weight: 600;
        color: #198754;
        margin-bottom: 3rem;
        padding-top: 3rem;
        text-align: center;
      }

      header {
        margin-bottom: 3rem;
      }

      header > p {
        font-size: 1.25rem;
        text-align: center;
        color: #6c757d;
      }

      h1 {
        padding: 0.2rem;
        color: #212529;
        text-align: center;
        font-size: 4.5rem;
        font-weight: 300;
      }

      h3 {
        color: #0d6efd;
        margin-bottom: 2rem;
        text-align: center;
        font-weight: 500;
        font-size: 1.75rem;
      }

      a {
        display: block;
        text-align: center;
        color: #212529;
      }

      ul {
        padding: 0;
        margin-top: 3rem;
        list-style: none;
        text-align: center;
      }

      .list {
        font-size: 1.25rem;
        font-weight: 600;
        margin-bottom: 1rem;
        padding: 0.2rem;
      }

      .product-name {
        font-size: 1.25rem;
        margin-bottom: 1rem;
        padding: 0.2rem;
        color: #212529;
        text-decoration: underline;
      }

      .original-price {
        color: #dc3545;
        font-size: 1.25rem;
        margin-bottom: 1rem;
        padding: 0.2rem;
        text-decoration: line-through;
      }

      .offer {
        border: 0.1rem solid #dc3545;
        color: #dc3545;
        margin-bottom: 1rem;
        padding: 0.5rem 1rem;
      }

      .current-price {
        color: #0d6efd;
      }

      .status {
        color: #198754;
      }

      .instock {
        color: #dc3545;
      }

      .info {
        font-size: 1.25rem;
        text-align: center;
        color: #0d6efd;
        text-decoration: underline;
      }

      .info-id {
        color: #dc3545;
        text-align: center;
        font-size: 1.25rem;
      }

      .info-extra {
        color: #6c757d;
        font-size: 1.25rem;
        text-align: center;
      }
    </style>
  </head>
  <body>
    <header>
      <h1>The Money Saving Helper</h1>
      <p>Let's see whether we can make some savings today....</p>
    </header>
    <main>
      <h3>Here is your search result:</h3>
      <% if (priceAlert.previousPrice.length) { %>
      <p>
        Woo-hoo!!! This product is available to purchase at a lower price ££££
      </p>
      <% } %>
      <img src="<%= priceAlert.imageUrl%>" alt="product image from Boots" />
      <ul>
        <li class="product-name"><%= priceAlert.name %></li>
        <li class="list current-price">
          Current Price: <%= priceAlert.currentPrice %>
        </li>
        <% if (priceAlert.previousPrice.length) { %>
        <li class="original-price">
          Previous Price: <%= priceAlert.previousPrice %>
        </li>
        <% } %> <% if (priceAlert.offer.length) { %>
        <li class="offer">OFFER: <%= priceAlert.offer %></li>
        <% } %> <% if (priceAlert.stockStatus === 'Add to basket') { %>
        <li class="list status">In Stock</li>
        <% } else { %>
        <li class="list instock">Not In Stock</li>
        <% } %>
      </ul>
    </main>
    <p class="info">Below is the ID Number for the price alert you created:</p>
    <p class="info-id"><%= priceAlert._id %></p>
    <p class="info-extra">
      (quote this number when you want to unsubscribe from this price alert)
    </p>
    <a href="/unsubscribe"> http://localhost:5000/unsubscribe </a>
  </body>
</html>
